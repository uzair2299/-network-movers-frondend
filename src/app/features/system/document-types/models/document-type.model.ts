export interface DocumentType {
  id: string;
  name: string;
  code: string;
  active: boolean;
  mandatory: boolean;
  expiryRequired: boolean;
  description: string;
  createdAt: string;
  updatedAt: string;
}
