export interface DocumentType {
  id: string;
  name: string;
  code: string;
  description: string;
  active: boolean;
  mandatory: boolean;
  expiryRequired: boolean;
  createdAt: string;
  updatedAt: string;
}
